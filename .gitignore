# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/

# ---- Large data / model artifacts (provide out-of-band; too big for git) ----
# Ontology databases required by step 2 (HGNC / MONDO / ChEBI are 30-500 MB each).
gpu_bundle/databases/*.json
gpu_bundle/databases/*.tsv
# NER corpus assembled by step 1 (thousands of XML, ~1 GB).
gpu_bundle/experimental_ner/
# Trained model checkpoints produced by step 2.
gpu_bundle/ppi-biobert-re/
gpu_bundle/ppi_data/
gpu_bundle/tmp_trainer/
tmp_trainer/

# ---- Step-1 generated corpora / intermediates (regenerated each run) ----
pmids/
high_impact_xmls/
ncbi_pdfs_grobid/
grobid_xmls/
named_entity_xmls/

# ---- Pipeline run trees & bundles ----
kaggle_working/
# Per-run HTML summaries (regenerated by each stage-1 script run).
/summaries/
gpu_bundle/GENETIC/
gpu_bundle/DISEASE/
gpu_bundle/CHEMICAL/
gpu_bundle/TRIPLES/
gpu_bundle/sentences/
gpu_bundle/summaries/
*.zip

# ---- Generated outputs (keep only the hand-authored docs) ----
# The per-run graph copy high_confidence.py writes to the root (named after the query).
/*_*.html
!step_1_publications.html
!step_2_triples.html
!step_3_graph.html
!step_4_diff.html
!requirements.html

# ---- OS / editor ----
.DS_Store
Thumbs.db
.idea/
.vscode/
